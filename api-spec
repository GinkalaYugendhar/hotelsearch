openapi: 3.0.1
info:
  title: Hotel Search
  version: v1
servers:
  - url: http://localhost:8080
    description: Local server

tags:
  - name: UserController
    description: Operations related to user management
  - name: HotelController
    description: Operations related to hotel management

paths:
  /users/adduser:
    post:
      tags:
        - UserController
      summary: Create a new user
      operationId: createUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/User'
      responses:
        '201':
          description: User created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /users/allusers:
    get:
      tags:
        - UserController
      summary: Get all users
      operationId: getAllUsers
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
        '404':
          description: No users found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /users/userhotels/{userId}:
    get:
      tags:
        - UserController
      summary: Get hotels for a user
      description: Fetch hotels based on user's city and pincode
      operationId: getHotelsByUserId
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Hotels found for user
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Hotel'
        '404':
          description: No hotels found for user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /users/login:
    post:
      tags:
        - UserController
      summary: Login user
      description: Validates username and password, returns matching hotels
      operationId: loginUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                userName:
                  type: string
                userPassword:
                  type: string
      responses:
        '200':
          description: Successfully logged in
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Hotel'
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /hotels/addhotel:
    post:
      tags:
        - HotelController
      summary: Create a new hotel
      operationId: createHotel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Hotel'
      responses:
        '201':
          description: Hotel created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Hotel'
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /hotels/allhotels:
    get:
      tags:
        - HotelController
      summary: Get all hotels
      operationId: getAllHotels
      responses:
        '200':
          description: List of hotels
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Hotel'
        '404':
          description: No hotels found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

components:
  schemas:
    User:
      type: object
      properties:
        userId:
          type: integer
          format: int64
          example: 1
        userName:
          type: string
          example: "alice123"
        userPassword:
          type: string
          example: "password123"
        name:
          type: string
          example: "Alice Johnson"
        city:
          type: string
          example: "Hyderabad"
        pinCode:
          type: string
          example: "500001"
    Hotel:
      type: object
      properties:
        hotelId:
          type: integer
          format: int64
          example: 101
        hotelName:
          type: string
          example: "Taj Banjara"
        hotelCity:
          type: string
          example: "Hyderabad"
        hotelAddress:
          type: string
          example: "Road No. 1, Banjara Hills, Hyderabad"
        pinCode:
          type: string
          example: "500001"
    ErrorResponse:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
          example: "2025-09-16T12:45:30Z"
        status:
          type: integer
          example: 404
        error:
          type: string
          example: "Not Found"
        message:
          type: string
          example: "No hotels found for the given user ID"
        path:
          type: string
          example: "/user-hotel-api/getUserId/1"